version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: apps/mmfv-backend/Dockerfile
    container_name: mmfv-backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
    restart: unless-stopped

